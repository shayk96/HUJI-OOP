/**
 * an interface for the different renderers types
 */
public interface Renderer {

    /**
     * the main function that renders the board
     * @param board the board to render
     */
    void renderBoard(Board board);

}
